/**
 * purgeOrphanedCvs.ts
 *
 * NOTE: Automatic Cloudinary deletion has been disabled to prevent
 * accidental deletion of active participant and applicant CV files.
 */

export const purgeOrphanedCvs = async () => {
    // Disabled for data safety
};

export const schedulePurgeJob = () => {
    // Disabled for data safety
};